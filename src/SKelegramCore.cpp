#include "./headers/SKelegramCore.hpp"

std::string SKelegramCore::elaborateRawData(std::string rawData){

    ML::log_info(std::string("RAW::INPUT -> ") + rawData, TARGET_ALL);

    return rawData;
};

